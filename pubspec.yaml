name: big_data_load_with_concurrency
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons:
  freezed_annotation: ^2.2.0
  json_annotation: ^4.9.0
  bloc:
  flutter_bloc:
  dartz:
  equatable:
  dio:
  get_it:
  pretty_dio_logger:
  connectivity_plus:
  riverpod:
  flutter_riverpod:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints:
  build_runner: ^2.3.3
  json_serializable: ^6.8.0
  freezed: ^2.5.7


flutter:

  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

